#' Generate random fake vehicle registration plate numbers.
#'
#' @param n number of observations.
#' @return A character vector of \code{n} randomly generated vehicle registration plate numbers.
#' @export
r_vehicle_registration_plate_numbers <- function(n) {
  return("To be updated: depends on Country.")
  }
